import 'package:flutter/material.dart';

class BookmarkWidgetView extends StatelessWidget {
  const BookmarkWidgetView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('BookMark'));
  }
}
