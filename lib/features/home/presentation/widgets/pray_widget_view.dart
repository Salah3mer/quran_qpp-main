import 'package:flutter/material.dart';

class PrayWidgetView extends StatelessWidget {
  const PrayWidgetView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pray'),
    );
  }
}
